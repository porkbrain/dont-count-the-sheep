[workspace]

resolver = "2"
members = ["meditation"]

[workspace.dependencies]
bevy = "0.12"

# compile deps packages in dev with optimizations
# this makes bevy run fast and bin recompile fast
[profile.dev.package."*"]
opt-level = 3
